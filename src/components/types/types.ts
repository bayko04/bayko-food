export interface ICards {
    img: string
    title: string
    text: string
    price: number
    id?: number
    weight?: string
}

export interface IFaq {
    question: string
    answer: string
    id?: number
}
